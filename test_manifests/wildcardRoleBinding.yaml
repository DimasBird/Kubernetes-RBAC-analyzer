apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: wildcard-binding
  namespace: wildcard-binding

subjects:
  - kind: User
    name: john

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: wildcard-role
