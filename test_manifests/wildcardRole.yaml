apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: wildcard-role
  namespace: wildcard

rules:
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["*"]

  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["*"]
